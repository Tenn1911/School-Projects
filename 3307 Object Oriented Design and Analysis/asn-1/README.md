# Simple File Manager  
	I was tasked to build my own simple file manager in C++, using the widget library  

## Installation  
	Clone the git and compile and run the code in a linux enviroment  

## Usage  
    The makefile will create an executable called FileManager when the make command is run  
	The use case is as follows: ./FileManager  